package yaboichips.etweaks.common.effects;

import net.minecraft.potion.Effect;
import net.minecraft.potion.EffectType;

public class SpiderManEffect extends Effect {
    public SpiderManEffect(EffectType typeIn, int liquidColorIn) {
        super(typeIn, liquidColorIn);
    }
}
